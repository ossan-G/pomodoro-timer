# ポモドーロタイマー 仕様書

本ツールは、ポモドーロ・テクニック（25分作業＋5分休憩）を用いた学習時間を計測し、その完了セット数を個人のGoogleスプレッドシートに自動記録するWebアプリケーションです。

## 1. ファイル構成

配布されたフォルダ内には、以下の2つのファイルが含まれています。

- `timer.html` : タイマーの画面と動作を制御する本体ファイル。
    
- `gascode.txt` : データを記録するためのバックエンド（Google Apps Script）のプログラムコード。
    

## 2. 動作要件

本ツールを動作させるには、利用者自身で以下の初期設定を行う必要があります。

1. **データベースの構築と連携**: ご自身のGoogleアカウントでスプレッドシートを作成し、`gascode.txt`の内容を設定して「ウェブアプリのURL」を発行します。その後、発行されたURLを `timer.html` 内の指定箇所に書き加える必要があります。（具体的な設定方法は同梱の「手順書」を参照してください）
    
2. **実行環境**: PCのブラウザ（Google Chrome推奨）で `timer.html` を開くことで動作します。
    

## 3. BGMの追加機能

本ツールは、作業中および休憩中に任意の音楽（BGM）をループ再生する機能を備えています。

**設定方法** お好きなMP3形式の音楽ファイルを用意し、`timer.html` と同じフォルダ内に以下のファイル名に変更して配置してください。

- 作業中（25分）用のBGM: ファイル名を `bgm1.mp3` にする
    
- 休憩中（5分）用のBGM: ファイル名を `bgm2.mp3` にする
    

※ ファイル名が完全に一致していないと再生されません。拡張子（.mp3）の重複などにご注意ください。BGMファイルが存在しない場合でも、タイマー自体の動作や記録機能には影響しません（無音で動作します）。