# ポモドーロタイマー 初期設定手順書

この手順書では、タイマーの記録を保存するためのデータベース（Googleスプレッドシート）を作成し、タイマー本体（`timer.html`）と連携させるまでの手順を解説します。

## ステップ1：データベース（スプレッドシート）の作成

1. ご自身のGoogleアカウントにログインした状態で、Googleドライブを開きます。
    
2. 左上の「新規」ボタンから「Google スプレッドシート」をクリックし、新しいシートを作成します。
    
3. 画面左上の「無題のスプレッドシート」をクリックし、任意の名前（例：`ポモドーロ記録`）に変更します。
    
4. シートの1行目に、記録用の見出しを入力します。（入力しなくても動作はしますが、後で見返す際に見やすくなります）
    
    - A1セルに `日時`
        
    - B1セルに `完了セット数`
        

## ステップ2：GAS（データ受信プログラム）の設定

1. 作成したスプレッドシートの画面上部メニューから「拡張機能」>「Apps Script」をクリックします。
    
2. 新しいタブでプログラムのエディタ画面が開きます。
    
3. 最初から入力されている数行の文字（`function myFunction() { ... }` など）を**すべて削除**して、画面を真っ白にします。
    
4. 同梱されている `gascode.txt` を開き、中身のテキストをすべてコピーします。
    
5. コピーしたテキストを、先ほど真っ白にしたエディタ画面に貼り付けます。
    
6. 画面上部にある「プロジェクトを保存」ボタン（フロッピーディスクのアイコン）をクリックします。
    

## ステップ3：URLの発行（デプロイ）

1. エディタ画面の右上にある青い「デプロイ」ボタンをクリックし、「新しいデプロイ」を選択します。
    
2. 左上の歯車アイコン（種類の選択）をクリックし、「ウェブアプリ」を選択します。
    
3. 右側の設定項目を以下のように**必ず**設定してください。ここを間違えると通信ができません。
    
    - **説明**: （空欄のままで構いません）
        
    - **実行するユーザー**: 「自分（あなたのメールアドレス）」
        
    - **アクセスできるユーザー**: **「全員」**（重要）
        
4. 右下の「デプロイ」ボタンをクリックします。
    

## ステップ4：アクセスの承認（初回のみ）

デプロイを実行すると、プログラムがスプレッドシートに書き込む許可を求める画面が表示されます。

1. 「アクセスを承認」をクリックします。
    
2. 自分のGoogleアカウントを選択します。
    
3. 「Googleが確認していません」という警告画面が出ます。左下の**「詳細」**をクリックします。
    
4. 下に現れる**「無題のプロジェクト（安全ではないページ）に移動」**をクリックします。
    
5. アクセスリクエストの画面を下へスクロールし、**「許可」**をクリックします。
    

## ステップ5：URLのコピーとHTMLの編集

1. 承認が完了すると「ウェブアプリ」の下に長いURL（`https://script.google.com/macros/s/...`）が表示されます。その下にある**「コピー」**をクリックします。（この画面は「完了」で閉じて構いません）
    
2. PCに保存されている `timer.html` を、「メモ帳」などのテキストエディタで開きます。（※Wordなどは使用しないでください）
    
3. コードの中から `const GAS_URL = '';` という行を探します。（40行目付近にあります）
    
4. シングルクォーテーション（`'`）の間に、先ほどコピーしたURLを貼り付けます。
    
    - 【変更前】 `const GAS_URL = '';`
        
    - 【変更後】 `const GAS_URL = 'https://script.google.com/macros/s/XXXXX/exec';`
        
5. `timer.html` を上書き保存して閉じます。
    

以上で設定は完了です。 `timer.html` をブラウザ（Google Chrome等）で開き、タイマーが終了した際にスプレッドシートにデータが記録されるか確認してください。